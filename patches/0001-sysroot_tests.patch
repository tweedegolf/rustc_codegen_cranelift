From 86743bd1ce3a5e3ef72b528ef8d14646e057187a Mon Sep 17 00:00:00 2001
From: bjorn3 <17426603+bjorn3@users.noreply.github.com>
Date: Tue, 8 Sep 2026 15:04:20 +0200
Subject: [PATCH] foo

---
 alloctests/Cargo.toml     |  1 +
 alloctests/benches/lib.rs |  1 +
 alloctests/foo/Cargo.toml | 15 +++++++++++++++
 alloctests/foo/lib.rs     |  2 ++
 alloctests/lib.rs         |  2 ++
 alloctests/tests/lib.rs   |  1 +
 6 files changed, 22 insertions(+)
 create mode 100644 alloctests/foo/Cargo.toml
 create mode 100644 alloctests/foo/lib.rs

diff --git a/Cargo.lock b/Cargo.lock
index 4609065..86aadc6 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -34,6 +34,7 @@ dependencies = [
 name = "alloctests"
 version = "0.0.0"
 dependencies = [
+ "foo",
  "rand",
  "rand_xorshift",
 ]
@@ -101,6 +102,14 @@ version = "0.2.0"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "77ce24cb58228fbb8aa041425bb1050850ac19177686ea6e0f41a70416f56fdb"
 
+[[package]]
+name = "foo"
+version = "0.0.0"
+dependencies = [
+ "rand",
+ "rand_xorshift",
+]
+
 [[package]]
 name = "fortanix-sgx-abi"
 version = "0.6.1"
diff --git a/alloctests/Cargo.toml b/alloctests/Cargo.toml
index 791b8ec..39f896b 100644
--- a/alloctests/Cargo.toml
+++ b/alloctests/Cargo.toml
@@ -17,6 +17,7 @@ doc = false
 [dev-dependencies]
 rand = { version = "0.9.0", default-features = false, features = ["alloc"] }
 rand_xorshift = "0.4.0"
+foo = { path = "foo" }
 
 [[test]]
 name = "alloctests"
diff --git a/alloctests/benches/lib.rs b/alloctests/benches/lib.rs
index 336e836..c4e157f 100644
--- a/alloctests/benches/lib.rs
+++ b/alloctests/benches/lib.rs
@@ -10,6 +10,7 @@
 #![deny(implicit_provenance_casts)]
 
 extern crate test;
+extern crate foo;
 
 mod binary_heap;
 mod btree;
diff --git a/alloctests/foo/Cargo.toml b/alloctests/foo/Cargo.toml
new file mode 100644
index 0000000..ab273c3
--- /dev/null
+++ b/alloctests/foo/Cargo.toml
@@ -0,0 +1,15 @@
+[package]
+name = "foo"
+version = "0.0.0"
+edition = "2024"
+
+[lib]
+path = "lib.rs"
+test = true
+bench = true
+doc = false
+crate-type = ["dylib"]
+
+[dependencies]
+rand = { version = "0.9.0", default-features = false, features = ["alloc"] }
+rand_xorshift = "0.4.0"
diff --git a/alloctests/foo/lib.rs b/alloctests/foo/lib.rs
new file mode 100644
index 0000000..855f8a6
--- /dev/null
+++ b/alloctests/foo/lib.rs
@@ -0,0 +1,4 @@
+#![feature(test)]
+extern crate rand;
+extern crate rand_xorshift;
+extern crate test;
diff --git a/alloctests/lib.rs b/alloctests/lib.rs
index 77e56d3..face505 100644
--- a/alloctests/lib.rs
+++ b/alloctests/lib.rs
@@ -80,6 +80,8 @@ extern crate std;
 #[allow(unused_imports)]
 use std::prelude::rust_2024::*;
 
+extern crate foo;
+
 #[cfg(test)]
 extern crate test;
 mod testing;
diff --git a/alloctests/tests/lib.rs b/alloctests/tests/lib.rs
index b2a3e8f..7f21abf 100644
--- a/alloctests/tests/lib.rs
+++ b/alloctests/tests/lib.rs
@@ -65,6 +65,7 @@
 // tidy-alphabetical-end
 
 extern crate alloc;
+extern crate foo;
 
 use std::hash::{DefaultHasher, Hash, Hasher};
 
diff --git a/alloctests/tests/c_str_alloc_error.rs b/alloctests/tests/c_str_alloc_error.rs
index 669a783..d2539a5 100644
--- a/alloctests/tests/c_str_alloc_error.rs
+++ b/alloctests/tests/c_str_alloc_error.rs
@@ -14,6 +14,8 @@
 #![cfg(not(all(miri, windows)))]
 #![feature(alloc_error_hook)]
 
+extern crate foo;
+
 use std::alloc::{GlobalAlloc, Layout, System, set_alloc_error_hook};
 use std::ffi::CString;
 use std::panic::{AssertUnwindSafe, catch_unwind};
diff --git a/alloctests/tests/vec_deque_alloc_error.rs b/alloctests/tests/vec_deque_alloc_error.rs
index 21a9118..866375d 100644
--- a/alloctests/tests/vec_deque_alloc_error.rs
+++ b/alloctests/tests/vec_deque_alloc_error.rs
@@ -1,5 +1,7 @@
 #![feature(alloc_error_hook, allocator_api)]
 
+extern crate foo;
+
 use std::alloc::{AllocError, Allocator, Layout, System, set_alloc_error_hook};
 use std::collections::VecDeque;
 use std::panic::{AssertUnwindSafe, catch_unwind};
diff --git a/alloctests/tests/fmt.rs b/alloctests/tests/fmt.rs
index 0989a56..3ec4a7a 100644
--- a/alloctests/tests/fmt.rs
+++ b/alloctests/tests/fmt.rs
@@ -283,6 +283,7 @@ fn test_format_args() {
     t!(s, "args were: hello world");
 }
 
+/*
 macro_rules! counter_fn {
     ($name:ident) => {
         fn $name() -> u32 {
@@ -321,6 +322,7 @@ fn test_once() {
 
     assert_eq!(format!("{0} {0} {0} {a} {a} {a}", count(), a = count()), "1 1 1 2 2 2".to_string());
 }
+*/
 
 #[test]
 fn test_refcell() {
diff --git a/alloctests/tests/vec.rs b/alloctests/tests/vec.rs
index f81db5b..0b70d27 100644
--- a/alloctests/tests/vec.rs
+++ b/alloctests/tests/vec.rs
@@ -545,6 +545,7 @@ fn test_cmp() {
 }
 
 #[test]
+#[cfg(false)]
 fn test_vec_truncate_drop() {
     struct_with_counted_drop!(Elem(i32), DROPS);
 
@@ -818,6 +819,7 @@ fn test_drain_end_overflow() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn test_drain_leak() {
     struct_with_counted_drop!(D(u32, bool), DROPS => |this: &D| if this.1 { panic!("panic in `drop`"); });
 
@@ -1057,6 +1059,7 @@ fn test_into_iter_clone() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn test_into_iter_leak() {
     struct_with_counted_drop!(D(bool), DROPS => |this: &D| if this.0 { panic!("panic in `drop`"); });
 
@@ -1269,6 +1272,7 @@ fn test_from_iter_specialization_panic_during_iteration_drops() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn test_from_iter_specialization_panic_during_drop_doesnt_leak() {
     struct_with_counted_drop!(
         Old(usize), DROP_COUNTER_OLD[|this: &Old| this.0, usize] =>
diff --git a/alloctests/tests/vec_deque.rs b/alloctests/tests/vec_deque.rs
index 15cc156..d2ac05a 100644
--- a/alloctests/tests/vec_deque.rs
+++ b/alloctests/tests/vec_deque.rs
@@ -718,6 +718,7 @@ fn test_show() {
 }
 
 #[test]
+#[cfg(false)]
 fn test_drop() {
     struct_with_counted_drop!(Elem, DROPS);
 
@@ -732,6 +733,7 @@ fn test_drop() {
 }
 
 #[test]
+#[cfg(false)]
 fn test_drop_with_pop() {
     struct_with_counted_drop!(Elem, DROPS);
 
@@ -750,6 +752,7 @@ fn test_drop_with_pop() {
 }
 
 #[test]
+#[cfg(false)]
 fn test_drop_clear() {
     struct_with_counted_drop!(Elem, DROPS);
 
@@ -767,6 +770,7 @@ fn test_drop_clear() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn test_drop_panic() {
     struct_with_counted_drop!(D(bool), DROPS => |this: &D| if this.0 { panic!("panic in `drop`"); } );
 
@@ -1616,6 +1620,7 @@ fn test_try_rfold_moves_iter() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn truncate_leak() {
     struct_with_counted_drop!(D(bool), DROPS => |this: &D| if this.0 { panic!("panic in `drop`"); } );
 
@@ -1636,6 +1641,7 @@ fn truncate_leak() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn retain_back_leak() {
     struct_with_counted_drop!(D(bool), DROPS => |this: &D| if this.0 { panic!("panic in `drop`"); } );
 
@@ -1656,6 +1662,7 @@ fn retain_back_leak() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn test_drain_leak() {
     struct_with_counted_drop!(D(u32, bool), DROPS => |this: &D| if this.1 { panic!("panic in `drop`"); } );
 
@@ -2430,6 +2437,7 @@ fn truncate_to_range_kept_straddles() {
 
 #[test]
 #[cfg_attr(not(panic = "unwind"), ignore = "test requires unwinding support")]
+#[cfg(false)]
 fn truncate_to_range_leak() {
     struct_with_counted_drop!(D(bool), DROPS => |this: &D| if this.0 { panic!("panic in `drop`"); } );
 
diff --git a/alloctests/lib.rs b/alloctests/lib.rs
index face505..a4c378a 100644
--- a/alloctests/lib.rs
+++ b/alloctests/lib.rs
@@ -105,12 +105,12 @@ pub(crate) mod test_helpers {
     /// Copied from `std::test_helpers::test_rng`, since these tests rely on the
     /// seed not being the same for every RNG invocation too.
     pub(crate) fn test_rng() -> rand_xorshift::XorShiftRng {
-        use std::hash::{BuildHasher, Hash, Hasher};
-        let mut hasher = std::hash::RandomState::new().build_hasher();
-        std::panic::Location::caller().hash(&mut hasher);
-        let hc64 = hasher.finish();
-        let seed_vec = hc64.to_le_bytes().into_iter().chain(0u8..8).collect::<std::vec::Vec<u8>>();
-        let seed: [u8; 16] = seed_vec.as_slice().try_into().unwrap();
-        rand::SeedableRng::from_seed(seed)
+        // use std::hash::{BuildHasher, Hash, Hasher};
+        // let mut hasher = std::hash::RandomState::new().build_hasher();
+        // std::panic::Location::caller().hash(&mut hasher);
+        // let hc64 = hasher.finish();
+        // let seed_vec = hc64.to_le_bytes().into_iter().chain(0u8..8).collect::<std::vec::Vec<u8>>();
+        // let seed: [u8; 16] = seed_vec.as_slice().try_into().unwrap();
+        rand::SeedableRng::from_seed([0; 16])
     }
 }
diff --git a/alloctests/tests/lib.rs b/alloctests/tests/lib.rs
index 7f21abf..9a46337 100644
--- a/alloctests/tests/lib.rs
+++ b/alloctests/tests/lib.rs
@@ -108,13 +108,13 @@ fn hash<T: Hash>(t: &T) -> u64 {
 /// Copied from `std::test_helpers::test_rng`, since these tests rely on the
 /// seed not being the same for every RNG invocation too.
 fn test_rng() -> rand_xorshift::XorShiftRng {
-    use std::hash::{BuildHasher, Hash, Hasher};
-    let mut hasher = std::hash::RandomState::new().build_hasher();
-    std::panic::Location::caller().hash(&mut hasher);
-    let hc64 = hasher.finish();
-    let seed_vec = hc64.to_le_bytes().into_iter().chain(0u8..8).collect::<Vec<u8>>();
-    let seed: [u8; 16] = seed_vec.as_slice().try_into().unwrap();
-    rand::SeedableRng::from_seed(seed)
+    // use std::hash::{BuildHasher, Hash, Hasher};
+    // let mut hasher = std::hash::RandomState::new().build_hasher();
+    // std::panic::Location::caller().hash(&mut hasher);
+    // let hc64 = hasher.finish();
+    // let seed_vec = hc64.to_le_bytes().into_iter().chain(0u8..8).collect::<Vec<u8>>();
+    // let seed: [u8; 16] = seed_vec.as_slice().try_into().unwrap();
+    rand::SeedableRng::from_seed([0; 16])
 }
 
 #[test]
diff --git a/alloctests/tests/collections/eq_diff_len.rs b/alloctests/tests/collections/eq_diff_len.rs
index ee1e294..703fc28 100644
--- a/alloctests/tests/collections/eq_diff_len.rs
+++ b/alloctests/tests/collections/eq_diff_len.rs
@@ -64,6 +64,7 @@ fn hashset_evil_eq() {
 }
 
 #[test]
+#[cfg(false)]
 fn hashmap_evil_eq() {
     let m1 = HashMap::from([(0, Evil)]);
     let m2 = HashMap::from([(0, Evil), (1, Evil)]);
diff --git a/alloctests/tests/vec.rs b/alloctests/tests/vec.rs
index 0b70d27..6550544 100644
--- a/alloctests/tests/vec.rs
+++ b/alloctests/tests/vec.rs
@@ -2766,6 +2766,7 @@ fn vec_null_ptr_roundtrip() {
 // when dealing with high-aligned Zero-Sized Types (ZSTs).
 use std::collections::{BTreeMap, BinaryHeap, HashMap, LinkedList, VecDeque};
 #[test]
+#[cfg(false)]
 fn zst_collections_iter_nth_back_regression() {
     #[repr(align(8))]
     #[derive(Debug, PartialEq, Eq, PartialOrd, Ord, Hash, Clone, Copy)]
-- 
2.47.3

